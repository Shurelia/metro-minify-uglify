{
  "name": "metro-minify-uglify",
  "version": "0.30.2",
  "description": "🚇 Default minifier for Metro, modified to not minify",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Shurelia/metro-minify-uglify"
  },
  "scripts": {
    "prepare-release": "test -d build && rm -rf src.real && mv src src.real && mv build src",
    "cleanup-release": "test ! -e build && mv src build && mv src.real src"
  },
  "license": "MIT",
  "dependencies": {
    "uglify-es": "^3.1.9"
  },
  "devDependencies": {},
  "peerDependencies": {
    "metro": "0.30.2"
  }
}
